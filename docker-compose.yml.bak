version: "3.8"
services:
  postgresdb:
    image: postgres
  upload.api:
    image: upload_api_image
    build:
      context: /src/
      dockerfile: FileUpload.Api/Dockerfile
  identityserver.api:
    image: identityserver_api_image
    build:
      context: .
      dockerfile: src/FileUpload.IdentityServer/Dockerfile
  aspnetcoremvc.web:
    image: aspnetcoremvc.web_image
    build:
      context: .
      dockerfile: src/FileUpload.MVC/Dockerfile

volumes:
  postgresdb_volume: